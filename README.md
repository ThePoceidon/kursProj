# Система учета файлов каталога

## Описание проекта

Данная программа представляет собой консольное приложение, реализованное на C++, предназначенное для управления и обработки каталога файлов.

Каждая запись в каталоге (`FileEntry`) включает в себя следующие данные:
1.  **Имя файла** (`std::string name`)
2.  **Дата создания** (`Date creationDate`)
3.  **Количество обращений** (`long long accessCount`)

## Поддерживаемый функционал

Приложение поддерживает следующий функционал, доступный через Главное меню:
1.  **Формирование каталога** (с клавиатуры или из файла)
2.  **Вывод текущего каталога** (отображение всех записей)
3.  **Удаление файлов по дате** (удаляются файлы, дата создания которых **МЕНЬШЕ** заданной)
4.  **Поиск** файла с **наибольшим количеством обращений**
5.  **Сохранение** текущего каталога в файл

---

## 🏗️ Структура репозитория

Проект организован в соответствии со стандартной структурой C++ проектов:  
├── include/ # Заголовочные файлы (.h)  
│ ├── FileCatalog.h  
│ └── Utils.h   
├── src/ # Файлы исходного кода (.cpp)   
│ ├── FileCatalog.cpp  
│ ├── Utils.cpp  
│ └── kursProj.cpp  
├── README.md # Этот файл  
└── kursProj.vcxproj # Файл проекта Visual Studio

### Размещение файлов:

* **Заголовочные файлы (*.h*)** находятся в папке: `include/`
* **Файлы исходного кода (*.cpp*)** находятся в папке: `src/`

---

## 🛠️ Сборка и запуск

Проект разработан и протестирован в среде **Microsoft Visual Studio**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <url>
    ```
2.  **Откройте проект:**
    Откройте файл *.vcxproj* в Visual Studio.
3.  **Настройка путей:**
    * Убедитесь, что в свойствах проекта (**C/C++ → Общие → Дополнительные каталоги включения**) добавлен путь к папке *include/*.
4.  **Сборка:**
    Соберите проект (Build → Build Solution или *Ctrl + Shift + B*).
5.  **Запуск:**
    Запустите исполняемый файл (*.exe*) из папки *Debug* или *Release* или непосредственно из Visual Studio (*F5*).

## ⚙️ Формат входного файла

При использовании функции "Создание каталога из файла" ожидается, что входной текстовый файл будет иметь следующий формат для каждой строки:

*Имя_файла / ДД.ММ.ГГГГ / Количество_обращений*

**Пример:**  
document.txt / 05.10.2023 / 15  
report_Q3.pdf / 28.02.2024 / 120  
config_ini / 10.01.2024 / 5